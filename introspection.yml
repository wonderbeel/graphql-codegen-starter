schema: ${SUBGRAPH_ADDRESS}
documents: "queries/**/*.{graphql,gql}"

extensions:
  codegen:
    generates:
      # Generate a schema.json file
      generated/schema.json:
        plugins:
          - "introspection"
